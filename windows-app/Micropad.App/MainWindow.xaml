<Window x:Class="Micropad.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Micropad" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundPrimary}">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Main Content -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Navigation -->
            <Border Grid.Column="0" Background="{StaticResource BackgroundSecondary}" 
                    BorderBrush="{StaticResource BorderColor}" BorderThickness="0,0,1,0">
                <StackPanel>
                    <TextBlock Text="MICROPAD" FontSize="18" FontWeight="Bold" 
                              Foreground="{StaticResource TextPrimary}" 
                              Margin="15,20,15,30"/>
                    
                    <ListBox x:Name="NavigationList" Background="Transparent" BorderThickness="0"
                            SelectionChanged="NavigationList_SelectionChanged">
                        <ListBoxItem Content="Devices" FontSize="14" Padding="15,10"/>
                        <ListBoxItem Content="Profiles" FontSize="14" Padding="15,10"/>
                        <ListBoxItem Content="Macros" FontSize="14" Padding="15,10"/>
                        <ListBoxItem Content="Stats" FontSize="14" Padding="15,10"/>
                        <ListBoxItem Content="Settings" FontSize="14" Padding="15,10"/>
                    </ListBox>
                </StackPanel>
            </Border>

            <!-- Content Frame -->
            <Frame x:Name="ContentFrame" Grid.Column="1" NavigationUIVisibility="Hidden" 
                   Background="{StaticResource BackgroundPrimary}"/>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="1" Background="{StaticResource BackgroundSecondary}" 
                BorderBrush="{StaticResource BorderColor}" 
                BorderThickness="0,1,0,0" Padding="10,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="{Binding StatusText}" 
                          Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding ConnectionStatus}" 
                          Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center" Margin="10,0"/>
                <TextBlock Grid.Column="2" Text="{Binding BatteryLevel}" 
                          Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="10,0"/>
                <TextBlock Grid.Column="3" Text="v1.0.0" 
                          Foreground="{StaticResource TextSecondary}" VerticalAlignment="Center" Margin="10,0"/>
            </Grid>
        </Border>
    </Grid>
</Window>
