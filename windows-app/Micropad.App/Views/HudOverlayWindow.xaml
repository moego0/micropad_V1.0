<Window x:Class="Micropad.App.Views.HudOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Micropad HUD" 
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize"
        Width="280" Height="80">
    <Border Background="#E6202020" CornerRadius="8" Padding="14,10" BorderBrush="#40FFFFFF" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect BlurRadius="12" ShadowDepth="2" Opacity="0.5"/>
        </Border.Effect>
        <TextBlock x:Name="HudText" Text="" FontSize="14" Foreground="White" TextWrapping="Wrap"/>
    </Border>
</Window>
