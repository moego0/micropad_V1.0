<Page x:Class="Micropad.App.Views.MacrosView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Macros"
      Background="{StaticResource BgPrimary}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource BgSecondary}"
                Padding="24" BorderBrush="{StaticResource Border}" BorderThickness="0,0,0,1">
            <Grid>
                <StackPanel>
                    <TextBlock Text="Macro Editor" FontSize="24" FontWeight="SemiBold"
                              Foreground="{StaticResource TextPrimary}"/>
                    <TextBlock Text="Record or manually create a sequence of actions"
                              FontSize="12" Foreground="{StaticResource TextSecondary}" Margin="0,6,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="Start Recording" Command="{Binding StartRecordingCommand}"
                            Background="{StaticResource Error}" Foreground="White"
                            Padding="14,8" Margin="0,0,8,0" BorderThickness="0" Cursor="Hand"
                            IsEnabled="{Binding IsNotRecording}"/>
                    <Button Content="Stop Recording" Command="{Binding StopRecordingCommand}"
                            Background="{StaticResource Success}" Foreground="White"
                            Padding="14,8" Margin="0,0,8,0" BorderThickness="0" Cursor="Hand"
                            IsEnabled="{Binding IsRecording}"/>
                    <Button Content="Add Delay" Command="{Binding AddDelayCommand}"
                            Background="{StaticResource BgTertiary}" Foreground="{StaticResource TextPrimary}"
                            Padding="14,8" Margin="0,0,8,0" BorderThickness="0" Cursor="Hand"/>
                    <Button Content="Clear" Command="{Binding ClearStepsCommand}"
                            Background="{StaticResource BgTertiary}" Foreground="{StaticResource TextPrimary}"
                            Padding="14,8" BorderThickness="0" Cursor="Hand"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Timeline (steps list) -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24">
            <ItemsControl ItemsSource="{Binding Steps}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource BgSecondary}"
                                BorderBrush="{StaticResource Border}" BorderThickness="1"
                                CornerRadius="8" Padding="16" Margin="0,0,0,8">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Background="{StaticResource AccentPrimary}"
                                        CornerRadius="6" Padding="10,6" Margin="0,0,12,0" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DisplayText}" FontSize="11" FontWeight="SemiBold"
                                              Foreground="White" MaxWidth="180" TextTrimming="CharacterEllipsis"/>
                                </Border>
                                <TextBlock Grid.Column="1" Text="{Binding DisplayText}" FontSize="13"
                                          Foreground="{StaticResource TextPrimary}" VerticalAlignment="Center"
                                          TextTrimming="CharacterEllipsis"/>
                                <Button Grid.Column="2" Content="Remove"
                                        Command="{Binding DataContext.RemoveStepCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}"
                                        Background="Transparent" Foreground="{StaticResource Error}"
                                        Padding="10,4" BorderThickness="0" Cursor="Hand"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Footer with stats -->
        <Border Grid.Row="2" Background="{StaticResource BgSecondary}"
                Padding="20" BorderBrush="{StaticResource Border}" BorderThickness="0,1,0,0">
            <Grid>
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Foreground="{StaticResource TextSecondary}" FontSize="12" VerticalAlignment="Center">
                        <Run Text="{Binding StepCount, Mode=OneWay}"/>
                        <Run Text=" steps"/>
                    </TextBlock>
                </StackPanel>
                <TextBlock Text="{Binding StatusText}" Foreground="{StaticResource TextTertiary}"
                          FontSize="11" VerticalAlignment="Center" HorizontalAlignment="Right"/>
            </Grid>
        </Border>
    </Grid>
</Page>
