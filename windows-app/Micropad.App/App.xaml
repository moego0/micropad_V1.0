<Application x:Class="Micropad.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:Micropad.App.Converters"
>
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="DesignSystem/DesignTokens.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <!-- Modern Dark Theme (UI/UX Improvements) -->
            <!-- Background -->
            <SolidColorBrush x:Key="BgPrimary" Color="#1E1E1E"/>
            <SolidColorBrush x:Key="BgSecondary" Color="#252526"/>
            <SolidColorBrush x:Key="BgTertiary" Color="#2D2D30"/>
            <SolidColorBrush x:Key="BgInput" Color="#3E3E42"/>

            <!-- Border & Divider -->
            <SolidColorBrush x:Key="Border" Color="#3F3F46"/>
            <SolidColorBrush x:Key="Divider" Color="#2E2E32"/>

            <!-- Text -->
            <SolidColorBrush x:Key="TextPrimary" Color="#FFFFFF"/>
            <SolidColorBrush x:Key="TextSecondary" Color="#CCCCCC"/>
            <SolidColorBrush x:Key="TextTertiary" Color="#999999"/>
            <SolidColorBrush x:Key="TextDisabled" Color="#6E6E6E"/>

            <!-- Accent (SENetLabs blue #00BFFF as brand accent) -->
            <SolidColorBrush x:Key="BrandBlue" Color="#00BFFF"/>
            <SolidColorBrush x:Key="AccentPrimary" Color="#0078D4"/>
            <SolidColorBrush x:Key="AccentHover" Color="#1E88E5"/>
            <SolidColorBrush x:Key="AccentPressed" Color="#0D5E9E"/>
            <SolidColorBrush x:Key="AccentGlow" Color="#4D0078D4"/>
            <!-- Selected slot glow (Macros grid) -->
            <SolidColorBrush x:Key="SlotSelectedBg" Color="#2A4A5C"/>

            <!-- Status -->
            <SolidColorBrush x:Key="Success" Color="#10B981"/>
            <SolidColorBrush x:Key="Warning" Color="#F59E0B"/>
            <SolidColorBrush x:Key="Error" Color="#EF4444"/>
            <SolidColorBrush x:Key="Info" Color="#3B82F6"/>

            <!-- Legacy keys (same theme, for existing views) -->
            <SolidColorBrush x:Key="BackgroundPrimary" Color="#1E1E1E"/>
            <SolidColorBrush x:Key="BackgroundSecondary" Color="#252526"/>
            <SolidColorBrush x:Key="BackgroundTertiary" Color="#2D2D30"/>
            <SolidColorBrush x:Key="BorderColor" Color="#3F3F46"/>
            <SolidColorBrush x:Key="AccentBlue" Color="#0078D4"/>

            <!-- Typography -->
            <FontFamily x:Key="MainFont">Segoe UI Variable Display, Segoe UI</FontFamily>
            <FontFamily x:Key="MonoFont">Cascadia Code, Consolas</FontFamily>

            <!-- Converters -->
            <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <converters:InverseBooleanToVisibilityConverter x:Key="InverseBoolToVisibility"/>
            <converters:InverseBooleanConverter x:Key="InverseBoolConverter"/>
            <converters:IntEqualsToBoolConverter x:Key="IntToBoolConverter"/>
            <converters:SlotSelectedToBrushConverter x:Key="SlotSelectedToBrushConverter"/>
            <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter"/>

            <!-- Dark ComboBox: dropdown and items use dark theme -->
            <Style TargetType="ComboBox">
                <Setter Property="Background" Value="{StaticResource BgSecondary}"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Border}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="8,6"/>
                <Setter Property="MinHeight" Value="28"/>
                <Setter Property="ItemContainerStyle">
                    <Setter.Value>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Background" Value="Transparent"/>
                            <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ComboBoxItem">
                                        <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
                                            <ContentPresenter/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsHighlighted" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource BgTertiary}"/>
                                            </Trigger>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="Bd" Property="Background" Value="{StaticResource AccentPrimary}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Setter.Value>
                </Setter>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <ToggleButton x:Name="ToggleButton" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                              BorderThickness="{TemplateBinding BorderThickness}" Focusable="False" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                              ClickMode="Press">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition/>
                                            <ColumnDefinition Width="20"/>
                                        </Grid.ColumnDefinitions>
                                        <ContentPresenter Grid.Column="0" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                          ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" HorizontalAlignment="Left" Margin="8,0,0,0" VerticalAlignment="Center"/>
                                        <Path Grid.Column="1" Data="M 0 0 L 4 4 L 8 0 Z" Fill="{StaticResource TextSecondary}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Grid>
                                </ToggleButton>
                                <ContentPresenter x:Name="ContentSite" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" IsHitTestVisible="False" Margin="8,0,24,0" VerticalAlignment="Center" HorizontalAlignment="Left"/>
                                <Popup x:Name="PART_Popup" AllowsTransparency="True" Focusable="False" IsOpen="{TemplateBinding IsDropDownOpen}" Placement="Bottom" PopupAnimation="Slide">
                                    <Border x:Name="DropDownBorder" Background="{StaticResource BgSecondary}" BorderBrush="{StaticResource Border}" BorderThickness="1" MinWidth="{TemplateBinding ActualWidth}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                        <ScrollViewer Margin="4,2" SnapsToDevicePixels="True">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- Default Button style: visible press feedback (flash) on click -->
            <Style TargetType="Button">
                <Setter Property="Padding" Value="12,8"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  TextBlock.Foreground="{TemplateBinding Foreground}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.92"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Bd" Property="Opacity" Value="0.75"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
