<Window x:Class="Micropad.App.Dialogs.ProfileConflictWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Profile conflict" Height="240" Width="420"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgPrimary}" ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" x:Name="TitleText" FontSize="16" FontWeight="SemiBold"
                  Foreground="{StaticResource TextPrimary}" TextWrapping="Wrap" Margin="0,0,0,12"/>
        <TextBlock Grid.Row="1" x:Name="MessageText"
                  Foreground="{StaticResource TextSecondary}" TextWrapping="Wrap" Margin="0,0,0,20"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Click="Cancel_Click" Width="90" Padding="12,8" Margin="0,0,8,0"
                    Background="{StaticResource BgTertiary}" Foreground="{StaticResource TextPrimary}" BorderThickness="0" Cursor="Hand"/>
            <Button Content="Pull (use device)" Click="Pull_Click" Width="120" Padding="12,8" Margin="0,0,8,0"
                    Background="{StaticResource AccentPrimary}" Foreground="White" BorderThickness="0" Cursor="Hand"
                    ToolTip="Replace PC copy with device version"/>
            <Button Content="Push (overwrite device)" Click="Push_Click" Width="150" Padding="12,8"
                    Background="{StaticResource Success}" Foreground="White" BorderThickness="0" Cursor="Hand"
                    ToolTip="Overwrite device with PC version"/>
        </StackPanel>
    </Grid>
</Window>
