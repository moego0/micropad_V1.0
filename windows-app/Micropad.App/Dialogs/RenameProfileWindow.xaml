<Window x:Class="Micropad.App.Dialogs.RenameProfileWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Rename profile" Height="160" Width="360"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgPrimary}" ResizeMode="NoResize">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Enter new profile name:" Foreground="{StaticResource TextSecondary}" Margin="0,0,0,10"/>
        <TextBox Grid.Row="1" x:Name="NameBox" Background="{StaticResource BgInput}" Foreground="{StaticResource TextPrimary}"
                 Padding="10,8" FontSize="14" VerticalContentAlignment="Center"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancel" IsCancel="True" Width="90" Padding="12,8" Margin="0,0,8,0"
                    Background="{StaticResource BgTertiary}" Foreground="{StaticResource TextPrimary}" BorderThickness="0" Cursor="Hand"/>
            <Button Content="OK" IsDefault="True" Width="90" Padding="12,8" Click="Ok_Click"
                    Background="{StaticResource AccentPrimary}" Foreground="White" BorderThickness="0" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>
